\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{amsmath}
\title{Worksheet \#2}
\author{Rosa Orellana}
\date{August 3, 2026}

\begin{document}

\maketitle
\begin{enumerate}
\item Compute $M_{\lambda,\mu}(e,m)$ for $\lambda= (2^2,1^4)$ and $\mu=(3^2,1^2)$.

\item Find and prove a formula for $M_{\lambda, 1^n}(e,m)$, for any $\lambda \vdash n$. 

\item Find and prove a formula for $M_{\lambda, n}(e,m)$, for any $\lambda \vdash n$. 

\item Find and prove a formula for $M_{\lambda, (n-1,1)}(e,m)$, for any $\lambda \vdash n$.

\item Prove or disprove: If $\lambda$ and $\mu$ are partitions with $|\lambda|=|\mu|$ and $\lambda>_{\text{lex}} \mu$, then $\mu'>_{\text{lex}} \lambda'$


\item Write the formal power series
\[ \prod_{i=1}^\infty (1+x_i+x_i^2)\]
in terms of the elementary symmetric functions. 

\item There is another combinatorial interpretation for $M_{\lambda, \mu} (e, m)$, if $\lambda, \mu \vdash k$, $M_{\lambda, \mu} (e, m)$ is equal to the number of $k\times k$ matrices with entries 0 or 1 only, such that the sum of the $i$-th row is $\mu_i$ and the sum of column $j$ is $\lambda_j$ for all $i$ and $j$. 
\begin{enumerate}
\item Use this combinatorial interpretation to find $M_{\lambda, \mu}(e, m)$ for $\lambda = (4,2,1)$ and $\mu = (3,1^4)$. 
\item Use this interpretation to show that $M_{\lambda,\mu}(e,m) = M_{\mu,\lambda}(e,m)$.
\item Describe a bijection from the tableaux interpretation to the $\{0,1\}$-matrix interpretation. 
\end{enumerate}

%\item Show that for all $k\geq 1$ and all partitions $\lambda, \mu\vdash k$, the quantity $M_{\lambda, \mu}(h,m)$ is the number of fillings of the Young diagram of $\lambda$ with positive integers for which the entries in each row are weakly increasing from left to right, and each integer $i$ appears $\mu_i$ times. 

\item Compute $M_{\lambda,\mu}(h,m)$ for $\lambda=(2^2,1^4)$ and $\mu=(3^2,1^2)$.

\item Find and prove a formula for $M_{\lambda, 1^n}(h,m)$, for any $\lambda \vdash n$. 

\item Find and prove a formula for $M_{\lambda, n}(h,m)$, for any $\lambda \vdash n$. 

\item Find and prove a formula for $M_{\lambda, (n-1,1)}(h,m)$, for any $\lambda \vdash n$.

\item Show that for all $k\geq 1$ and all partitions $\lambda, \mu\vdash k$, the quantity $M_{\lambda, \mu}(p,m)$ is the number of fillings of the Young diagram of $\lambda$ with positive integers for which the entries in each row are constant, and each integer $i$ appears $\mu_i$ times. 

\item Compute $M_{\lambda,\mu}(p,m)$ for $\lambda=(2^2,1^4)$ and $\mu=(3^2,1^2)$.

\item Find and prove a formula for $M_{\lambda, 1^n}(p,m)$, for any $\lambda \vdash n$. 

\item Find and prove a formula for $M_{\lambda, n}(p,m)$, for any $\lambda \vdash n$. 

\item Find and prove a formula for $M_{\lambda, (n-1,1)}(p,m)$, for any $\lambda \vdash n$.



\end{enumerate}
\section*{Español}
\begin{enumerate}
\item Calcule $M_{\lambda,\mu}(e,m)$ para $\lambda= (2^2,1^4)$ y $\mu=(3^2,1^2)$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, 1^n}(e,m)$, para cualquier $\lambda \vdash n$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, n}(e,m)$, para cualquier $\lambda \vdash n$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, (n-1,1)}(e,m)$, para cualquier $\lambda \vdash n$.
\item Pruebe o dé un contraejemplo: Si $\lambda$ y $\mu$ son particiones con $|\lambda|=|\mu|$ y $\lambda>_{\text{lex}} \mu$, entonces $\mu'>_{\text{lex}} \lambda'$.
\item Escriba la serie de potencias formal \[ \prod_{i=1}^\infty (1+x_i+x_i^2)\] en términos de las funciones simétricas elementales.
\item Existe otra interpretación combinatoria para $M_{\lambda, \mu} (e, m)$: si $\lambda, \mu \vdash k$, $M_{\lambda, \mu} (e, m)$ es igual al número de matrices $k\times k$ cuyas entradas son únicamente 0 o 1, tales que la suma de la $i$-ésima fila es $\mu_i$ y la suma de la columna $j$ es $\lambda_j$ para todos $i$ y $j$.
\begin{enumerate}
\item Use esta interpretación combinatoria para encontrar $M_{\lambda, \mu}(e, m)$ para $\lambda = (4,2,1)$ y $\mu = (3,1^4)$.
\item Use esta interpretación para demostrar que $M_{\lambda,\mu}(e,m) = M_{\mu,\lambda}(e,m)$.
\item Describa una biyección entre la interpretación como tablas y la interpretación como matrices de $\{0,1\}$.
\end{enumerate}
\item Calcule $M_{\lambda,\mu}(h,m)$ para $\lambda=(2^2,1^4)$ y $\mu=(3^2,1^2)$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, 1^n}(h,m)$, para cualquier $\lambda \vdash n$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, n}(h,m)$, para cualquier $\lambda \vdash n$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, (n-1,1)}(h,m)$, para cualquier $\lambda \vdash n$.
\item Demuestre que para todo $k\geq 1$ y todas las particiones $\lambda, \mu\vdash k$, $M_{\lambda, \mu}(p,m)$ es el número de \underline{rellenos de Tablas de Young de $\lambda$} \textbf{UNSURE} con enteros positivos, en los cuales las entradas de cada fila son constantes y cada entero $i$ aparece $\mu_i$ veces.
\item Calcule $M_{\lambda,\mu}(p,m)$ para $\lambda=(2^2,1^4)$ y $\mu=(3^2,1^2)$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, 1^n}(p,m)$, para cualquier $\lambda \vdash n$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, n}(p,m)$, para cualquier $\lambda \vdash n$.
\item Encuentre y pruebe una fórmula para $M_{\lambda, (n-1,1)}(p,m)$, para cualquier $\lambda \vdash n$.
\end{enumerate}
\end{document}
